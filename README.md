# uncertainty-aware-survival-tcga
Reproducible survival modeling pipeline (TCGA-HNSC) with uncertainty quantification, calibration and biologically-informed DDR score.

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python src/run_pipeline.py
